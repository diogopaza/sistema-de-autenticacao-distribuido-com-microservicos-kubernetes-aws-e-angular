server:
  port: 8080

spring:
  application:
    name: gateway-service

  cloud:
    gateway:
        discovery:
          locator:
            enabled: true
            lower-case-service-id: true

# nas linhas abaixo inclusao manual dos servicos no gateway
#  cloud:
#    gateway:
#        routes:
#          - id: auth-service
#            uri: lb://AUTH-SERVICE
#            predicates:
#              - Path=/auth/**
#            filters:
#              - StripPrefix=1




management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
